<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="microbiology.taman.microbiology" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/appicon"

        android:label="@string/app_name"
        android:logo="@drawable/logo"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAINACTIVITY" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Loading"
            android:label="@string/title_activity_loading"
            android:theme="@style/AppTheme" >
        </activity>
        <activity
            android:name=".BacteriaList_activity"
            android:label="@string/title_activity_bacteria_list_activity"
            android:theme="@style/AppTheme" >
        </activity>
        <activity
            android:name=".BacteriaPropertyList"
            android:label="@string/title_activity_bacteria_property_list"
            android:theme="@style/AppTheme" >
        </activity>
    </application>

</manifest>
